@model Searcher.Models.SearchModel
@using (Html.BeginForm("DoSearchInResults", "Home", FormMethod.Get))
{
    @Html.TextBoxFor(x => x.SearchText)

    <input type="submit" value="Искать" class="btn btn-primary" />
}